---
name: review-status
description: Show the current review loop status and configuration.
---

Run this bash command and present the output to the user in a readable format:

```bash
bash ~/.claude/hooks/review-loop.sh status
```
